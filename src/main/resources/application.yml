spring:
    application:
        name: server
    datasource:
        url: jdbc:mysql://localhost:3306/crypto?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
        username: crypto
        password: crypto1234!
        driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
        hibernate:
            ddl-auto: none   # (create, update, validate, none)
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect
#    sql:
#        init:
#            mode: always
